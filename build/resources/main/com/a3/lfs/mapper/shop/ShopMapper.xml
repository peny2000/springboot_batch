<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC
        "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.a3.lfs.mapper.shop.ShopMapper">

    <select id="findAllOriginalShop" resultType="Shop">

        select
            kunnr,
            name1,
            ort01,
            stras,
            telf1,
            telf2,
            regio,
            regio_txt,
            vkbur,
            vkbur_txt,
            pltyp,
            pltyp_txt,
            konda,
            konda_txt,
            J_1KFREPRE as shp_owner,
            CONCAT(kunnr, '@neps.lfcorp.com') as shp_mail
        FROM SAPSR3.ZSDV035
    </select>

</mapper>